/target

# wasm-pack output. Generated by the build step in the README; the sheet
# degrades to server-side scores without it rather than breaking.
/web/pkg

# Browser-automation captures from development. These contain real
# contributor names and email addresses read out of scanned repositories,
# so they must never be committed.
/.playwright-mcp
